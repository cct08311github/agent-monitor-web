// Minimal Babel config for Jest to resolve babel.config.js
// All source is CommonJS and needs no transformation — this file just
// satisfies babel-jest's requirement that babel.config.js be present.
module.exports = {
    presets: [],
};
